on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: A job to tiecd
    steps:
      - name: ls
        run: ls -al
      - name: pwd
        run: eho `pwd`
      - name: Deploy
        id: hello
        uses: dataaxiom/tiecd-deploy-action@main
        with:
          who-to-greet: 'Mona the Octocat'
